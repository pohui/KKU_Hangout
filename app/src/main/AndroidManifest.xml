<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hangout">

    <application
        android:name=".data.App"
        android:allowBackup="true"
        android:icon="@drawable/iconkkuhangoutfullyshadowed"
        android:label="@string/app_name"
        android:roundIcon="@drawable/iconkkuhangoutfullyshadowed"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hangout">

        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Hangout.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Hangout.NoActionBar" />

                <activity android:name=".LoginActivity"
                    android:theme="@style/Theme.Hangout.NoActionBar"/>
    </application>

</manifest>